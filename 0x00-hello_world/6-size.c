#include <stdio.h>
/**
 * main - prints size of various data types
 * Return: Always 0
 */
int main(void)
{
	printf("Size of a char is=%i bytes\n", size of(char));
	printf("Size of a int is=%i bytes\n", size of(int));
	printf("Size of a long int is=%i bytes\n", size of(long int));
	printf("Size of a long long int is=%i bytes\n", size of(long long int));
	printf("Size of a float is=%i bytes\n", size of(float));

	return (0);
}
